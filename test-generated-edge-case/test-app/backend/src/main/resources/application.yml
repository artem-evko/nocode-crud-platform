server:
  port: 8080
spring:
  application:
    name: test-app
  datasource:
    url: jdbc:postgresql://localhost:5432/test-app
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true